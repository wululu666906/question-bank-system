# 智能题库生成系统 - 全新迭代架构开发计划书

## 一、 需求润色与核心设计理念

根据您的原生需求，为您提炼并润色了以下系统级架构设计规范：

> **核心目标**：构建一个高精准度、长篇幅、支持复杂题型并附带最新互联网真实素材的自动化试卷生成链路。
> 
> **工作流串联规范**：
> 1. **全景输入中枢**：系统起点，接收核心命题参数（必填：知识点/题目主体、难度[简/中/难/专家]、生成数量、岗位/职业、题型[单选/多选/判断/主观/材料/作文]）。
> 2. **意图拓展与外脑检索 (DeepSeek + Custom Crawler)**：设立首个 DeepSeek 调度大脑，专职“意图识别与拓展”。将用户模糊的自然语言具象化。配合内置的专业 Python 爬虫探针（突破常规防爬），精准抓取全网最新、最优质的硬核时效性素材，绝不允许凭空捏造。
> 3. **防发散安全锁 (Code)**：设立格式护城河节点，严格锁定题型、数量边界及难度基准，防止大模型后续输出“跑题”或“格式幻觉”。
> 4. **深度单抽循环引擎 (Iteration + DeepSeek)**：为了确保每一道题的篇幅、解析深度与逻辑严密性，废弃“一次性生成多题”的浅层做法。引入**迭代循环节点 (Iteration)**。循环次数由前端传入的【数量】变量控制；循环内部封装第二个 DeepSeek 命题模型，它吸收外网素材和安全锁规则，一次只专注打磨 **1** 道题。
> 5. **终极质检与渲染修正 (Code)**：单题生成后进入流出前的“质检清洗间”。由纯代码执行三项使命：①强制语种校验（非中文强转为中文）；②严格 Markdown 语法重构；③注入底层渲染转义代码（完美兼容并正确显示数学公式与各类特殊符号）。
> 6. **流式推递 (End)**：打通底层数据流，确保平台最终以类似 ChatGPT 的打字机节奏（均匀舒适），将打磨完毕的专业题目平滑流式推送给用户终端。

---

## 二、 预计成品图 (工作流 DAG 控制流向图)

您可以将以下流程图视为您在 Dify 画布上即将看到的“成品节点拓扑图”：

```mermaid
graph TD
    A([Start 节点<br/>输入: 考点/难度/题型/数量/职业]) --> B(意图识别大脑<br/>DeepSeek-V3);
    B --> C(Python 专业爬虫<br/>抓取最新真实资料);
    A --> D(约束锁节点<br/>生成死格式规则);

    subgraph 迭代引擎节点丛集 [按数量循环: Iteration]
        E([迭代内始端]) --> F(单题命题大脑<br/>DeepSeek-V3);
        F --> G(质检渲染修复<br/>代码清洗中/公式修正);
        G --> H([迭代内终端]);
    end

    C -- 传入优质素材 --> E;
    D -- 传入安全锁 --> E;
    A -- 传入数量决定循环次数 --> 迭代引擎节点丛集;

    迭代引擎节点丛集 --> I([End 节点<br/>流式平滑输出]);
    
    style A fill:#e1f5fe,stroke:#01579b
    style B fill:#fff3e0,stroke:#e65100
    style C fill:#f3e5f5,stroke:#4a148c
    style D fill:#f3e5f5,stroke:#4a148c
    style 迭代引擎节点丛集 fill:#e8f5e9,stroke:#1b5e20
    style F fill:#fff3e0,stroke:#e65100
    style G fill:#f3e5f5,stroke:#4a148c
    style I fill:#e1f5fe,stroke:#01579b
```

---

## 三、 节点实现细节 (User Review Required)

为了将上述架构完美落地为 Dify DSL 源文件，有以下核心策略需要向您确认：

1. **意图检索方案**：由于 Dify 的单大模型节点不能“自主挂载执行 Python 爬虫模块”（非 Agent 模式），我将拆分为两个节点：**[大模型 A 负责发散搜索词] -> [Python 代码负责执行 Jina 爬取]**。这完全符合您的要求，且代码节点会执行最专业的防爬应对策略。
2. **迭代器限制**：Dify 的迭代节点 (Iteration) 规定其输入必须是一个 **Array (数组)**。我们会利用代码节点，将用户的 `question_count`（例如 5）转换成一个长度为 5 的数组 `[1,2,3,4,5]`，从而精准控制大模型循环出题 5 次，最后再将结果合并。
3. **公式级渲染**：质检节点中的 Python 代码会自动识别并转义 LaTeX 数学公式（将 `\\[` 转换为 `$$` 等渲染器支持的标准 Markdown），实现您要求的排版零错误。

> [!IMPORTANT]
> **请审阅以上计划书与节点图。如果确认该设计完全符合您的蓝图，请回复“确认”或给出修改意见，我将立即生成并校验为您专属的最终 DSL 文件。**
